# Задание:
# Создайте генератор gen = (x for x in range(3)).
# Пройдитесь по нему дважды с помощью двух for-циклов.
# Объясните в комментарии, почему второй цикл не выведет ничего.
# Это проверяет понимание того, что генератор можно "исчерпать".

gen = (x for x in range(3))

first_run = []
for x in gen:
    first_run.append(x)

second_run = []
for x in gen:
    second_run.append(x)

# Вопрос для студента:
# Почему second_run пустой? Напишите свой ответ ниже в комментарии:
# ...