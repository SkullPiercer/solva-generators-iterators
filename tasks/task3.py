# Задание:
# Напишите генератор read_lines(filename), который открывает файл и
# возвращает строки по одной за итерацию (лениво).
# Строки не нужно очищать от \n.
# Функция должна работать с любым файлом (не читайте всё в память).
# Можно использовать with open(...) внутри генератора.

def read_lines(filename):
    pass